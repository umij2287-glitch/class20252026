<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/context 
						http://www.springframework.org/schema/context/spring-context.xsd
						http://mybatis.org/schema/mybatis-spring 
						http://mybatis.org/schema/mybatis-spring.xsd">
	
	<context:component-scan base-package="com.demoweb.dao"></context:component-scan>
	
	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- new org.apache.tomcat.jdbc.pool.DataSource() 한 것과 같음. -->
	<bean id="dataSource" class="org.apache.tomcat.jdbc.pool.DataSource">
		<property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
		<property name="url" value="jdbc:mysql://127.0.0.1:3306/labdb" />
		<property name="username" value="human" />
		<property name="password" value="human" />
		<property name="initialSize" value="10" />
		<property name="maxActive" value="20" />
		<property name="maxIdle" value="10" />
	</bean>
	
	<!-- SqlSessionFactoryBean.getObject() 를 호출해서 SqlSessionFactory 객체를 만드는 bean 설정 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="classpath:mybatis-config.xml" />
	</bean>
	
	<!-- SqlSessionFactory 를 사용해서 SqlSession 객체를 만들고 SqlSession 객체를 감싸는 SqlSessionTemplate 객체 생성 -->
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSessionFactory" />
	</bean>
	
	
	<!-- MemberMapper 인터페이스를 구현하는 클래스를 자동 생성해서 그 객체를 컨테이너에 등록 -->
	<!-- @Mapper annotation 으로 대체 id 는 자동으로 MemberMapper 에서 첫 글자를 소문자로 memberMapper 가 됨.-->
	<!-- 
	<bean id="memberMapper" class="org.mybatis.spring.mapper.MapperFactoryBean">
		<property name="sqlSessionFactory" ref="sqlSessionFactory" />
		<property name="mapperInterface" value="com.demoweb.mapper.MemberMapper"/>
	</bean>
	 -->
	 <!-- com.demoweb.mapper 패키지 안에 포함된 클래스, 인터페이스 등을 읽고 어노테이션을 처리. -->
	 <mybatis:scan base-package="com.demoweb.mapper"></mybatis:scan>
	 
</beans>


















