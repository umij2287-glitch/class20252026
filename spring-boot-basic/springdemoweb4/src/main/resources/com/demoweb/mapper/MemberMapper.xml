<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace : 패키지이름.클래스이름 -->
<mapper namespace="com.demoweb.mapper.MemberMapper">
	
	<insert id="insertMember" parameterType="com.demoweb.dto.MemberDto">
		insert into tbl_member (memberid, passwd, email) values(#{memberId}, #{passwd}, #{email})
	</insert>
	
	<select id="checkMember" parameterType="hashmap" resultType="com.demoweb.dto.MemberDto">
		select memberId, email, userType, joinDate, active 
		from tbl_member where memberId = #{memberId} and passwd = #{passwd}
	</select>
	
	<select id="selectMemberCountById" parameterType="String" resultType="int"> 
		select count(*) from tbl_member where memberid = #{memberId}
	</select>
	
</mapper>

